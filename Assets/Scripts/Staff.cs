using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Staff : baseCard
{
    // Start is called before the first frame update
    void Start()
    {
        cardType = CardType.STAFF;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
