using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Swordsman : baseCard
{
    // Start is called before the first frame update
    void Start()
    {
        cardType = CardType.SWORDSMAN;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
