using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Fireball : baseCard
{
    // Start is called before the first frame update
    void Start()
    {
        cardType = CardType.FIREBALL;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
